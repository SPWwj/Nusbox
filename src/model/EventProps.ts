export enum Event {
    timetable,
    userEvent,
    appointment,
    planting,
    joinRoom,
    unkown
}

export enum EventType {
    add,
    edit,
    delete,
    seeding,
    seedRequest,
    seedInfo,
    seedAccept
}